# quorum

Things to add in the future:
- Conditional rules (i.e. `X/X => 1 if X != 0`)
    - Mini VM to quickly check conditions
    - Branch/forking system that displays multiple results depending on assumptions
    - Manually add global assumptions
- Manual substitutions (i.e. `pi = 3.145`)
- Different types of backend number values (i.e. `i64`, `f64`, `Ratio<i64>`)
- Disabling/enabling folding of operators or functions
    - Add support for trig functions
    - Add support for max/min functions
    - Add support for logarithmic functions
- Add logging using the `log` crate